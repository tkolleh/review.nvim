*review.txt*  Code review annotations for codediff.nvim

Author: TJ Kolleh
License: Apache-2.0

==============================================================================
CONTENTS                                                     *review-contents*

    1. Overview .............. |review-overview|
    2. Requirements .......... |review-requirements|
    3. Commands .............. |review-commands|
    4. Configuration ......... |review-config|
    5. Keymaps ............... |review-keymaps|
    6. Highlight Groups ...... |review-highlights|
    7. Export Format ......... |review-export|
    8. Storage ............... |review-storage|

==============================================================================
1. OVERVIEW                                                  *review-overview*

review.nvim adds code review annotations on top of |codediff.nvim|: leave
comments (note, suggestion, issue, praise) on lines in a diff view, then
export them to clipboard or sidekick.nvim for AI chat. See the README for
full feature list and workflow: https://github.com/tkolleh/review.nvim

This is a hard fork of georgeguimaraes/review.nvim (v2.0.0), rewriting the
storage layer onto DuckDB for multi-writer support and shifting direction
toward general-purpose multi-author commenting. See the README for details.

==============================================================================
2. REQUIREMENTS                                          *review-requirements*

- Neovim >= 0.11
- codediff.nvim (https://github.com/esmuellert/codediff.nvim)
- nui.nvim (https://github.com/MunifTanjim/nui.nvim)
- `duckdb` CLI on your $PATH (https://duckdb.org), used for comment storage

==============================================================================
3. COMMANDS                                                 *review-commands*

All commands use the `:Review` prefix.

                                                                     *:Review*
:Review                     Open codediff diff view with review keymaps.
                            Shows staged and unstaged changes.

:Review open                Same as `:Review`.

                                                             *:Review-commits*
:Review commits             Open commit picker (last 50 commits). Use
                            `<Space>` to select a range, `<CR>` to confirm.

:Review commits {rev1} {rev2}
                            Review a specific revision range directly,
                            skipping the picker.

                                                               *:Review-close*
:Review close               Export comments to clipboard and close the
                            review tab.

                                                              *:Review-export*
:Review export              Export comments to clipboard and open a
                            preview split.

                                                             *:Review-preview*
:Review preview             Preview exported markdown in a vertical split.

                                                            *:Review-sidekick*
:Review sidekick            Send comments to sidekick.nvim.

                                                                *:Review-list*
:Review list                List all comments with a picker to jump to any.

                                                               *:Review-clear*
:Review clear               Clear all comments.

==============================================================================
4. CONFIGURATION                                              *review-config*

Call `require("review").setup(opts)` with any of the following options.
All fields are optional; defaults are shown.
>lua
    require("review").setup({
      comment_types = {
        note = {
          key = "n",
          name = "Note",
          icon = "📝",
          hl = "ReviewNote",
          line_hl = "ReviewNoteLine",
        },
        suggestion = {
          key = "s",
          name = "Suggestion",
          icon = "💡",
          hl = "ReviewSuggestion",
          line_hl = "ReviewSuggestionLine",
        },
        issue = {
          key = "i",
          name = "Issue",
          icon = "⚠️",
          hl = "ReviewIssue",
          line_hl = "ReviewIssueLine",
        },
        praise = {
          key = "p",
          name = "Praise",
          icon = "✨",
          hl = "ReviewPraise",
          line_hl = "ReviewPraiseLine",
        },
      },

      keymaps = { ... },  -- see |review-keymaps|
    })
<

==============================================================================
5. KEYMAPS                                                   *review-keymaps*

All keymaps can be customized via `opts.keymaps`. Set a key to `false` to
disable it. Keymaps are buffer-local to the review diff buffers, which stay
locked against edits for the whole review; closing it restores each
buffer's original editability.

COMMENTS                                           *review-keymaps-comments*

    Key                 Description ~
    `<localleader>c`      Add comment (opens type picker)       |n, x|
    `<localleader>f`      Add file-level comment                |n|
    `<localleader>e`      Edit comment at cursor                |n|
    `<localleader>d`      Delete comment at cursor              |n|

NAVIGATION                                               *review-keymaps-nav*

    Key         Description ~
    `]n`          Next comment in file                  |n|
    `[n`          Previous comment in file              |n|
    `c`           List all comments                     |n|

ACTIONS                                               *review-keymaps-actions*

    Key         Description ~
    `C`           Export to clipboard + preview          |n|
    `S`           Send to sidekick.nvim                  |n|
    `<C-r>`       Clear all comments                     |n|
    `q`           Export & close review                   |n|
    `?`           Show help popup                         |n|

Provided by codediff.nvim itself, not review.nvim: `]f`/`[f` (next/previous
file), `<leader>b` (toggle file panel), `t` (toggle layout), `g?` (help).

POPUP                                                 *review-keymaps-popup*

These keymaps are active inside the comment popup window:

    Key         Description ~
    `<C-s>`       Submit comment                         |i, n|
    `<CR>`        Submit comment (normal mode only)      |n|
    `q`           Cancel (normal mode)                   |n|
    `<Esc>`       Cancel (normal mode)                   |n|
    `<Tab>`       Cycle comment type                     |i, n|

==============================================================================
6. HIGHLIGHT GROUPS                                      *review-highlights*

These highlight groups are defined with default links. Override them after
calling setup() or in a |ColorScheme| autocommand.

  Group                   Default link      Usage ~
  `ReviewNote`              DiagnosticInfo    Note virtual text and sign
  `ReviewSuggestion`        DiagnosticHint    Suggestion virtual text and sign
  `ReviewIssue`             DiagnosticWarn    Issue virtual text and sign
  `ReviewPraise`            DiagnosticOk      Praise virtual text and sign
  `ReviewSign`              Comment           Sign column marker (●)
  `ReviewVirtText`          Comment           General virtual text
  `ReviewPickerHash`        Identifier        Commit hash in picker
  `ReviewPickerMeta`        Comment           Author/date in picker
  `ReviewPickerSelected`    String            Selected commit marker

  Group                   Default bg        Usage ~
  `ReviewNoteLine`          #0d1f28           Note line background
  `ReviewSuggestionLine`    #152015           Suggestion line background
  `ReviewIssueLine`         #28250d           Issue line background
  `ReviewPraiseLine`        #15152a           Praise line background

A comment box's border (the `╭─╮`/`│ │`/`╰─╯` characters only -- not the
header label or comment text, which stay on the type highlight above) is
additionally colored per author. This is always on and not configurable.
The color is deterministic per author string, adapts to |'background'|
automatically, and is rendered through a lazily-registered highlight group
named `ReviewAuthorBorder_{hex}` (created on first use, cached thereafter).

==============================================================================
7. EXPORT FORMAT                                              *review-export*

Comments are exported as markdown optimized for AI consumption. The format
includes file path, line numbers, diff side (old/new), comment type, and
surrounding code context. Use `:Review export` to preview.

==============================================================================
8. STORAGE                                                   *review-storage*

Comments are stored in a DuckDB file per branch or revision range: >
    ~/.local/share/nvim/review/{project-hash}-{branch}.duckdb
<
Safe for concurrent writers (you, a teammate, an AI agent). Sessions expire
after 7 days. See the README's Features section for details.

The `review_comments` table has `color_dark`/`color_light` columns,
GENERATED ALWAYS AS columns that DuckDB computes at insert time by hashing
the `author` column into a fixed palette. Since DuckDB cannot add a
generated column to an already-existing table, this only takes effect for
sessions created after upgrading -- run `:Review-clear` to reset an older
session's storage file if it's missing author colors.

==============================================================================
 vim:tw=78:ts=8:ft=help:norl:
